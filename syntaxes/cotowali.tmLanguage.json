{
	"$schema": "https://raw.githubusercontent.com/martinring/tmlanguage/master/tmlanguage.json",
	"name": "Cotowali",
	"patterns": [
		{
			"include": "#keywords"
		},
		{
			"include": "#strings"
		}
	],
	"repository": {
		"keywords": {
			"patterns": [{
				"name": "keyword.control.cotowali",
				"match": "\\b(assert|require|if|else|while|for|in|continue|break|fn|return|yield|var|type|namespace)\\b"
			}]
		},
		"strings": {
      "patterns": [{
        "name": "string.quoted.double.cotowali",
        "begin": "\"",
        "end": "\"",
        "patterns": [
          {
            "name": "constant.character.escape.cotowali",
            "match": "\\\\."
          }
        ]
      }, {
        "name": "string.quoted.single.cotowali",
        "begin": "'",
        "end": "'",
        "patterns": [
          {
            "name": "constant.character.escape.cotowali",
            "match": "\\\\."
          }
        ]
      }]
    }
  },
	"scopeName": "source.cotowali"
}
